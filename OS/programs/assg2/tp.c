// /**
//  * @file tp.c
//  * @author your name (you@domain.com)
//  * @brief 
//  * @version 0.1
//  * @date 2023-01-20
//  * 
//  * @copyright Copyright (c) 2023
//  * 
//  */
// #include <stdio.h>
// #include <stdlib.h>
// #include <string.h>

// void remove_leading_space(char *dest, char *src) {
//     int idx = 0, k = 0;

//     while (src[idx] == ' ' || src[idx] == '\t') {
//         idx++;
//     }
//     int l = strlen(src);
//     for (int i = idx; i < l; i++) {
//         dest[k] = src[i];
//         k++;
//     }
//     dest[k] = '\0';
// }


int main(int argc, char const *argv[])
{
    // char str[]="ls -lsa > hello.txt";
    // char *array[16];
    // char *args[16];
    
    // char *p = strtok(str, ">");
    // char *q;
    // int i = 0;
    // while (p != NULL) {
    //     array[i++] = p;
    //     p = strtok(NULL, ">");
    // }

    // char cmd[64], file[64];
    // strcpy(cmd, array[0]);
    // strcpy(file, array[1]);
    // i = 0;
    // p = strtok(cmd, " ");
    // while (p != NULL) {
    //     args[i++] = p;
    //     p = strtok(NULL, " ");
    // }


    // printf("%s__%s\n", array[0], array[1]);

    // char a[16] = "   ls -lsa abc";
    // char b[16];
    // strcpy(b, a);
    // remove_leading_space(a, b);
    // printf("%s\n%s\n", a, b);
    printf("Hahahahahahaha\n");
    return 0;
}
